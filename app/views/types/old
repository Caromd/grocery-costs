<div id="index_wrapper">
  <div id="form">
    <%= form_for([@type, @types.build]) do |f| %>
	    <%= f.text_field :name, placeholder: "New Type..." %>
	    <%= f.submit %>
    <% end %>
  </div>
</div>

  <div class="actions"><%= f.submit %></div>